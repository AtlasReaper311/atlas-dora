{
  "name": "atlas-dora",
  "version": "0.1.0",
  "private": true,
  "description": "DORA metrics for the Atlas Systems estate, computed from its own public endpoints. Read-only downstream consumer.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "lint": "eslint .",
    "check": "npm run lint && npm test && wrangler types --include-runtime false --check && wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "eslint": "^10.8.1",
    "vitest": "^4.1.10",
    "wrangler": "^4.120.0"
  }
}
